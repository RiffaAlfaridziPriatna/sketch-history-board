{
  "name": "sketch-history-board",
  "version": "1.0.0",
  "description": "A web application for drawing sketches with version history",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "db:start": "./scripts/start-db.sh",
    "db:stop": "docker-compose -f docker-compose.dev.yml down",
    "db:logs": "docker-compose -f docker-compose.dev.yml logs postgres",
    "setup": "npm run install:all && npm run db:start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
